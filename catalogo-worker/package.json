{
  "name": "posweb-catalogo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 execute posweb-catalogo --local --file=./migrations/0001_create_productos.sql",
    "migrate:remote": "wrangler d1 execute posweb-catalogo --remote --file=./migrations/0001_create_productos.sql"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260721.1",
    "wrangler": "^4.14.1"
  }
}
